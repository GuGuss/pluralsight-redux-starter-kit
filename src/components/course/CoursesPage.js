import React, { PropTypes } from 'react';

// Is not a stateless component (const) because of some current Hot Reloading limitations.
class CoursesPage extends React.Component {
  render() {
    return (
      <div>
        <h1>Courses</h1>
      </div>
    );
  }
}

export default CoursesPage;